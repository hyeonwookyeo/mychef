<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MagaReply">

	<select id="slist" parameterType="int" resultType="magareply">
		select * from maga_re where maga_num=#{maga_num} order by magare_num
	</select>
	
	<insert id="insert" parameterType="magareply">
		<selectKey keyProperty="magare_num" order="BEFORE" resultType="int">
			select nvl(max(magare_num),0) + 1 from maga_re
		</selectKey>
		insert into maga_re values (#{magare_num},#{maga_num},#{id},#{re_content},sysdate,'y',#{maga_num},0,0)	
	</insert>
	
	<delete id="delete" parameterType="int">
		delete from maga_re where magare_num=#{magare_num}
	</delete>
</mapper>