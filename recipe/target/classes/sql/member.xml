<?xml version="1.0" encoding="UTF-8" ?>
 <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberns">
 
   
    <!-- 로그인 인증 -->
    <select id="login_check" parameterType="String" resultType="member">
     select * from member where id=#{id} and state=1
    </select>
    
      <!-- 회원삭제 -->
    <update id="member_delete" parameterType="member">
      update member set deldate=#{deldate}, state=2,
      deldate=sysdate where id=#{id}
    </update>
    
        <!-- 회원수정 -->
    <update id="member_edit" parameterType="member">
     update member set pwd=#{pwd},name=#{name},
     zip=#{zip},addr1=#{addr1},
     addr2=#{addr2},phone=#{phone1},
     email=#{email},profile=#{profile,jdbcType=VARCHAR} 
     where id=#{id}
    </update>    

</mapper>